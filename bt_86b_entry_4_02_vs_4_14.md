# bt #86b — 4/02 vs 4/14 진입 차이 분석 (정정판)

> ⚠️ **정정 (2026-06-01)**: 초판은 4/07~4/14 매도를 "사용자 수동매도"로 결론냈으나,
> 봇 로그(`btc_bot.log.1`) 검증 결과 **봇 자체 자동청산이 오라벨된 것**으로 확정. 정정함.

## 핵심 질문
bt_86 시뮬은 4/02 진입(101.6M)→+13.7% 익절. 실거래 "첫 확정거래"는 4/14(108.66M)→손절.
그 사이 봇이 왜 그 포지션을 못 끌고 갔나? "수동매도"는 진짜 사용자 개입이었나?

## 판정: **라벨 오류(버그) — 봇이 스스로 자동청산. 사용자 수동매도 아님.**
봇 미가동(X) · 사용자 개입(X) · **매도 기록 버그로 자동청산이 "수동매도"로 오기록(O, v20.6 이전)**.

## 근거 1 — 봇은 ~4/02 진입까지 정상 수행 (불변)
- 4/07 1차익절가 104,799,000 (`ATRx1.8`) → 진입가 역산 ≈ 101.6M = 시뮬 4/02 진입가(101,576,000) 일치.
- 봇은 4/02 저점 진입 기회를 놓치지 않았다. 문제는 그 다음 "청산"이 왜 빨랐나.

## 근거 2 — "수동매도" 4건의 봇 로그상 실제 트리거 (btc_bot.log.1)
| btc_trade.csv 기록 | 봇 로그 실제 트리거 | 판정 |
|---|---|---|
| 4/07 02:04 수동매도 +0.63% | 01:07 SIGTERM 재시작, 02:00 보유중, **청산 트리거 로그 없음** | 불명 |
| 4/08 13:01 수동매도 +0.70% | `Time Exit: 횡보 Time Exit (13봉/실질:+0.42%)` | **봇 자동청산** |
| 4/12 11:05 수동매도 +0.03% | `🚨 인트라캔들 하드스톱 발동` + `[ERROR] 매도 주문 실패 — 사유:ATR트레일링` | **봇 자동청산** |
| 4/14 23:39 수동매도 +2.38% | `🚨 인트라캔들 하드스톱 발동 (111,306,000 <= 111,382,159)` | **봇 자동청산** |

- 전체 4월 로그에 `수동 (전량) 매도 감지` 라인 **0건** — `_detect_manual_trade`는 텔레그램만 보내고 logger 미기록.
- 4건 중 최소 3건이 **Time Exit / 인트라캔들 하드스톱** = 봇 자동청산.

## 근거 3 — 오라벨 메커니즘 (v20.6 이전 매도 기록 버그)
- `_detect_manual_trade`는 v205에도 `_partial_selling` 가드 존재 → 정상 봇 매도는 못 잡음.
- 그러나 봇의 하드스톱/TimeExit 매도가 **API 주문 실패(4/12 명시)** 하거나 정상 기록 경로를 못 타면
  `status.in_position=True` + 잔고≈0 상태가 남고 → 다음 루프에서 fallback `_detect_manual_trade`가
  잡아 **"수동매도(전량)"** 로 오기록 (logger 미출력, 텔레그램 경고만).
- **v20.6 (4/19 배포) 팬텀 가드 수정** 이후로는 정상 `사유:인트라캔들하드스톱` 기록.
  → 4/19 18:52 이후 4건(4/19·5/04·5/11·5/16)은 모두 정상 라벨. 4/07~4/14 4건만 오라벨.

## 결론 (정정)
- **사용자 수동매도 아님.** 4/07~4/14 "수동매도(전량)"는 v20.6 이전 매도-기록 버그로
  **봇의 자동청산(Time Exit 횡보 / 인트라캔들 하드스톱)이 오라벨된 것**.
- 시뮬(+13.7%)과 실거래의 진짜 차이: ① 당시 봇(v18.4/v20.5)에 **Time Exit(횡보, 13봉)** 이 있어 추세 초입을 조기 컷
  (현 v20.9.17·bt_86 시뮬엔 Time Exit 없음 — 이미 제거됨), ② 하드스톱 조기 발동. 둘 다 **봇 자체 동작**.
- 운영/사용자 문제 아님. **5월 손실 원인은 진입필터(#85 기각)·청산로직(#86 기각)도, 수동개입도 아니다.**
  당시 봇의 조기청산(Time Exit) + 매도기록 버그가 겹친 결과이며, **Time Exit 는 v20.0+ 에서 이미 제거**되어 현행엔 비해당.
- **잔여 액션**: btc_trade.csv 의 2026-04-19 이전 `수동매도` 라벨은 신뢰 불가(자동청산 오라벨) → 과거 데이터 분석 시 제외.
