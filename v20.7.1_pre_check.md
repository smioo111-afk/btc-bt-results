# v20.7.1 Kill Switch 자동복구 사전 점검

**작성일**: 2026-04-19
**대상**: btc_bot_v207.py
**범위**: Kill Switch 자동복구 로직 + 24h 재발동 방지 + 시뮬 동기화

## 점검 결과 요약

| 영역 | 상태 | 비고 |
|---|---|---|
| 1. Kill Switch 발동 코드 | 🟢 확인됨 | 2개 경로 (MDD L3112 + PF L1595) |
| 2. Kill Switch 체크 위치 | 🟢 확인됨 | 진입(L3291), 피라미딩(L2032), Reinvest(L2190) |
| 3. 수동 해제 명령 | 🟢 확인됨 | /killswitch off L2544-2552 |
| 4. MDD 회복 추적 | 🟢 가능 | current_mdd 필드 활성, L1590 |
| 5. 발동 이력 기록 | 🟡 부분 | kill_switch_reason 있음, 시간 이력 없음 (신규 필드 필요) |

**🔴 중대 이슈 0건, 🟡 경미 1건 (status 신규 필드). Phase 2 진행 가능.**

## 1. Kill Switch 발동 코드 위치

### MDD-based (L3110-3119)
```python
mdd_exceeded, mdd_val = self._update_mdd(equity)
if mdd_exceeded and not self.status.get("kill_switch", False):
    self.status["kill_switch"]        = True
    self.status["kill_switch_reason"] = f"MDD {mdd_val:.1%} >= {MDD_STOP_PCT:.0%}"
    self._save_status()
    tg_error(...)
```
- `_update_mdd` (L1582): peak 대비 -20% 체크
- peak_equity 는 status["mdd_peak_equity"] 영속화

### PF-based (L1595-1605)
```python
def _check_kill_switch(self):
    if self.status.get("kill_switch", False):
        return True, self.status.get("kill_switch_reason", "이미 발동")
    stats = calc_recent_stats(n=KILL_SWITCH_MIN_TRADES)  # 20거래
    if stats["trade_count"] < KILL_SWITCH_MIN_TRADES:
        return False, ""
    if stats["pf"] < KILL_SWITCH_PF and stats["winrate"] is not None:
        return True, reason
```

**→ 자동복구 로직은 두 경로 이후에 실행되어야 함.** 
MDD 체크 블록(L3112-L3119) 이후 또는 PF 체크(L3122-L3131) 이후에 auto-recover 블록 추가.

## 2. Kill Switch 체크 위치

- 신규 진입 (L3291 `ks_active = self.status.get("kill_switch", False)`)
- 피라미딩 TP (L2032 `_pyr_ks = self.status.get("kill_switch", False)`)
- Reinvest (L2190 `if ks_active: logger.info("잔액 투입 스킵: Kill Switch 발동 중")`)

**→ status["kill_switch"]가 False로 돌아가면 즉시 모든 체크 통과. 자동복구 로직은 단순히 플래그 off만 하면 됨.**

## 3. 수동 해제 명령 (L2544-2552)

```python
elif cmd == "/killswitch":
    if len(parts) > 1 and parts[1].lower() == "off":
        self.status["kill_switch"]        = False
        self.status["kill_switch_reason"] = ""
        self._save_status()
        tg_info(f"✅ *Kill Switch 해제* | {now}")
```

**→ 자동복구와 병존 가능**. 자동복구는 조건 충족 시 프로그램이 해제, 수동은 사용자 개입. 둘 다 kill_switch=False 설정만 하면 됨.

추가 권고: /killswitch status 조회 명령, /killswitch reset (24h 카운터 리셋) 신규 추가.

## 4. MDD 회복 추적

`_update_mdd` (L1582):
```python
def _update_mdd(self, equity):
    peak = self.status.get("mdd_peak_equity", 0.0)
    if equity > peak or peak <= 0:
        self.status["mdd_peak_equity"] = equity
        self.status["current_mdd"] = 0.0
        return False, 0.0
    mdd = (peak - equity) / peak
    self.status["current_mdd"] = mdd
    if mdd >= MDD_STOP_PCT:
        return True, mdd
    return False, mdd
```

**→ current_mdd 필드가 매 봉 업데이트됨. 자동복구 트리거(`current_mdd < 0.10`)는 _update_mdd 호출 직후 체크 가능.**

주의: peak_equity는 영구 증가만. kill_switch 해제 후에도 peak은 유지되므로, 자동복구는 peak 대비 회복을 요구.

## 5. 발동 이력 추적 — 🟡 신규 필드 필요

현재 status:
- `kill_switch`: bool
- `kill_switch_reason`: str

**없음**:
- 발동 시각 (24h 재발동 방지용)
- 자동복구 시각
- 24h 누적 발동 횟수

**신규 status 필드 (v20.7.1)**:
```python
"last_killswitch_at":         0.0,  # 마지막 발동 epoch time
"last_killswitch_reason":     "",   # 마지막 발동 사유 (기존 reason과 별도 기록)
"last_killswitch_recovered_at": 0.0, # 마지막 자동복구 epoch time
"killswitch_count_24h":       0,    # 최근 24h 발동 횟수 (1로 초과 시 영구 중단)
```

## Phase 2 구현 계획

### 작업 1: 자동복구 로직 (main loop에 추가)

L3131 이후(Kill Switch 체크 후)에 추가:
```python
# Kill Switch 자동복구 (v20.7.1)
if self.status.get("kill_switch", False):
    self._check_auto_recover(equity, mdd_val)
```

`_check_auto_recover` 메서드:
- `current_mdd < 0.10` (회복)
- `time.time() - last_killswitch_at >= 86400` (24h 경과)
- `killswitch_count_24h < 2` (24h 내 2회 발동 시 영구 중단)

### 작업 2: 24h 재발동 방지

발동 블록(L3112, L3122)에 다음 체크 추가:
```python
# 24h 내 재발동 시 영구 중단 플래그
last_ks = self.status.get("last_killswitch_at", 0)
within_24h = (time.time() - last_ks) < 86400
if within_24h:
    self.status["killswitch_count_24h"] = self.status.get("killswitch_count_24h", 0) + 1
else:
    self.status["killswitch_count_24h"] = 1
self.status["last_killswitch_at"] = time.time()
```

### 작업 3: 텔레그램 명령 확장 (/killswitch status, /killswitch reset)

### 작업 4: 시뮬레이터 24h 재발동 방지

simulator.py의 KS_AUTO 모드에 재발동 guard 추가. 현재는 즉시 재발동 가능.

### 작업 5: 버전/파일 전략

**권고**: 신규 파일 `btc_bot_v208.py` + BOT_VERSION = "20.8"
- CLAUDE.md 파일 버전 관리 규칙 준수
- 깨끗한 rollback 가능
- 변경 범위 작지만 safety 우선

대안: in-place v207.py 수정 + BOT_VERSION "20.7.1"
- 변경 최소
- 하지만 rollback 시 git reset 필요

## Phase 2 체크리스트

1. btc_bot_v207.py → btc_bot_v208.py 복사
2. BOT_VERSION "20.7" → "20.8"
3. 헤더 v20.8 변경 블록 추가
4. status default 신규 4 필드 추가
5. _check_auto_recover 메서드 추가
6. 발동 블록 2곳 (MDD / PF)에 24h 재발동 체크 추가
7. main loop 자동복구 호출 추가
8. /killswitch 명령 3종 확장
9. simulator.py auto_recover 모드에 24h guard 추가
10. systemd ExecStart v207 → v208
11. CLAUDE.md, btc_bot_improvement.md, improvement_todo.md 갱신
12. 배포 (stop/reload/start)
13. 검증 (로그, status, 텔레그램)
14. commit

**Phase 1 종료. Phase 2 자동 진행 가능.**
